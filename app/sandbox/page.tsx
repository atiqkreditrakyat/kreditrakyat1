import SandboxHero from "@/sandbox/Hero";

export const metadata = {
    title: "Sandbox | Kredit Rakyat",
    description: "Sandbox for testing new components",
};

export default function SandboxPage() {
    return (
        <main className="min-h-screen">
            <SandboxHero />
        </main>
    );
}
